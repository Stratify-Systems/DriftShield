- id: POL-IAM-001
  name: Enforce User MFA
  service: iam
  severity: HIGH
  description: IAM users with console access must have MFA enabled.
  remediation: Assign an MFA device to the IAM user in AWS Console.
  conditions:
    all:
      - field: mfa_enabled
        operator: equals
        value: true
